program beauty;

{$APPTYPE CONSOLE}
uses
  SysUtils;

var

Begin
  assign (input,'beauty.in');
  assign (output,'beauty.out');
  reset(input);
  rewrite(output);
  readln (n);
  for i:=1 to

  close(input);
  close(output);
end.
