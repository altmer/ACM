program corporate;

{$APPTYPE CONSOLE}
uses
  SysUtils;

var n : integer = 1;
    a : array [1..4000] of string;
    Cur : array [0..200] of boolean;

procedure UnAdd (a, b : string; var cur : array of boolean);

begin
      
end;


begin
 assign (input,'corporate.in');
 assign (output,'corporate.out');
 reset(input);
 rewrite(output);
 while n<>0 then
 begin
  readln (n);
  for i:=1 to n do readln (s[i]);
  
         


 end; 

 close(input);
 close(output);
end.
